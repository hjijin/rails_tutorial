<h1>Edit user</h1>
<%= form_for(@user) do |f| %>
	<%= render 'fields', :f => f %>
	<div class="action">
		<%= f.submit "Update" %>
	</div>
<% end %>
<!-- 登陆gravatar修改头像 -->
<div>
	<%= gravatar_for @user %>
	<a href="http://gravatar.com/emails" target=_blank>change<a>
</div>